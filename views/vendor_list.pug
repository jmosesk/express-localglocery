extends layout

block content
  h1= title
  
  ul
    each vendor in vendor_list
      li 
        a(href=vendor.url) #{vendor.name} 
        |  (#{vendor.address}) | (#{vendor.phone})

    else
      li There are no vendors.